import { FC } from 'react';

interface IPropsTypes {
  name?: string;
}

const Logos: FC<IPropsTypes> = (props) => {
  return (
    <div className="">
      <h1>Logos</h1>
    </div>
  );
};

export default Logos;
