import { FC } from 'react';

interface IPropsTypes {
  name?: string;
}

const FormContact: FC<IPropsTypes> = (props) => {
  return (
    <div className="">
      <h1>FormContact</h1>
    </div>
  );
};

export default FormContact;
